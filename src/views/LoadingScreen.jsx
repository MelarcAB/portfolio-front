// LoadingScreen.js

function LoadingScreen() {
    return (
        <div className="fixed inset-0 flex items-center justify-center z-50 bg-white">
            {/* Aquí puedes poner cualquier contenido para tu pantalla de carga, como un logo o un spinner */}
            <div>Cargando...</div>
        </div>
    );
}

export default LoadingScreen;
